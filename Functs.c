//its a crappy experiment
int s,q,t,z;//god this got messy


void int_swap(int x, int y){

	*x = s;
	*y = z;
    &z = t;
	s* = q;
	t* =s;
	s //I gotta stop lmao
	//check byte order for the real func but I was trying to swap bits with just pointers
	//and not Bitwise Operators. Lol It turned messy real quick
}
