#this is an attempt at recursive I.E BufferOverFlow Check
#for my programmers this is becoming a security source code.
#use as an regular import file edit and use at yo risk.
import sys

x = 0
y = 0

class RecurveForms:
    def __init__(self, 2cAddition,usbAddition):
        self.2cAddition
        self.usbAddition



    def 2cAddition(): 
        if x + y -2**32>= 2**32:#postive overflow
            print("BufferOverFlow")
            sys.exit()
        if 2**31 >=x+y:
            print("BufferOverFlow")
            sys.exit()
        if x+y == -2**31 <= x+y < 2**31:         
            sys.flush()
        if x+y + 2**32 == x + y<-2**31:
            print("BufferOverFlow")
            sys.exit()

        def usbAddition():
            if x + y == x+y <2**32:
                sys.flush
            if x + y -2**32 and 2**32 <= x + y < 2**33:
                print("OverFlow")
                sys.exit
